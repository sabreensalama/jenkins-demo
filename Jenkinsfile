#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('Build App') {
            steps {
                script {
                    tags_extra = "value_1"
                }
               echo "Start Building the APP................................................"
               
            }
        }
        stage('Deploy App') {
            steps {
                echo "Start Deploying the APP................................................"
            }
        }
    }
}
